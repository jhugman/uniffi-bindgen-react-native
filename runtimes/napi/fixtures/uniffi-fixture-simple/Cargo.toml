[workspace]

[package]
name = "uniffi-fixture-simple"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
uniffi = { version = "0.31", features = ["cli"] }
thiserror = "2"
tokio = { version = "1", features = ["rt", "sync"] }
async-trait = "0.1"
